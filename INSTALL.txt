In order to install and build our system, you should first 
unzip the project folder somewhere you can navigate to in the terminal. 
You can then navigate to the project folder, where you will need to first install
Bogue. You can do this by typing in the terminal "opam install bogue". Then all
you need to do is type "dune exec bin/main.exe" and enjoy!